using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WriteFile : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //sxr.WriteHeaderToTaggedFile("main", "rxntime");
        //sxr.WriteToTaggedFile("main", gameObject.name);
        //sxr.WriteToTaggedFile("main", (Time.time * 1000).ToString());
    }

    // Update is called once per frame
    void Update()
    {

    }
}
